web: echo "=== Starting migrations ===" && python manage.py migrate --verbosity 2 && echo "=== Migrations completed ===" && gunicorn wsgi:application --bind 0.0.0.0:$PORT
release: python manage.py migrate --verbosity 2

