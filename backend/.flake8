[flake8]
# Совместимость с black (длина строки 88 символов)
max-line-length = 88
# Игнорируем только правила, которые конфликтуют с black
# E203 - пробелы перед двоеточием (black форматирует по-своему)
# E266 - несколько # перед комментарием (black форматирует по-своему)
# E501 - длинные строки (black форматирует до 88 символов, но мы уже настроили max-line-length)
# W503 - перенос строки перед оператором (black использует W503 стиль)
# E402 - импорты не в начале файла (нормально для Django скриптов с django.setup())
# E226 - пробелы вокруг операторов (black форматирует автоматически)
# W391 - пустая строка в конце файла (не критично, можно игнорировать)
# E122 - неправильный отступ в продолжении строки (black форматирует автоматически)
ignore = E203, E266, W503, E402, E226, W391, E122
# Исключаем директории и файлы с base64 данными
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    migrations,
    settings.py,
    update_base64_25.py,
    update_pink_carnations_25.py,
    upload_carnations_manual.py,
    replace_pink_25.py,
    replace_pink_25_simple.py
