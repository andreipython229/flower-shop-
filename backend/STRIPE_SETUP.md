# Настройка Stripe для оплаты заказов

## 1. Установка зависимостей

```bash
pip install stripe==10.12.0
```

Или установите все зависимости из `requirements.txt`:
```bash
pip install -r requirements.txt
```

## 2. Получение API ключей Stripe

1. Зарегистрируйтесь на [Stripe](https://stripe.com)
2. Перейдите в [Dashboard](https://dashboard.stripe.com)
3. В разделе **Developers** → **API keys** получите:
   - **Publishable key** (начинается с `pk_test_` для тестового режима)
   - **Secret key** (начинается с `sk_test_` для тестового режима)

## 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта `backend/` или установите переменные окружения:

```bash
# Windows PowerShell
$env:STRIPE_PUBLISHABLE_KEY="pk_test_..."
$env:STRIPE_SECRET_KEY="sk_test_..."
$env:STRIPE_WEBHOOK_SECRET="whsec_..."  # Получите после настройки webhook
```

Или добавьте в `.env` файл:
```
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
```

## 4. Настройка Webhook (для обновления статуса заказа)

1. В Stripe Dashboard перейдите в **Developers** → **Webhooks**
2. Нажмите **Add endpoint**
3. Укажите URL: `http://localhost:8000/api/webhook/` (для разработки)
4. Выберите события:
   - `checkout.session.completed` - успешная оплата
   - `checkout.session.async_payment_failed` - неудачная оплата
5. Скопируйте **Signing secret** (начинается с `whsec_`) и добавьте в переменные окружения

## 5. Создание миграций

После добавления поля `payment_intent_id` в модель `Order`:

```bash
python manage.py makemigrations orders
python manage.py migrate
```

## 6. Тестирование

### Тестовые карты Stripe:

- **Успешная оплата**: `4242 4242 4242 4242`
- **Отклонённая карта**: `4000 0000 0000 0002`
- **Требуется 3D Secure**: `4000 0025 0000 3155`

**Любая дата истечения** в будущем, **любой CVC** (например, `123`)

## 7. Проверка работы

1. Запустите сервер: `python manage.py runserver`
2. Откройте фронтенд и оформите заказ
3. Нажмите кнопку "💳 Оплатить онлайн"
4. Введите тестовые данные карты
5. После успешной оплаты статус заказа обновится на "completed"

## Важные замечания

- В **тестовом режиме** используйте тестовые ключи (`pk_test_`, `sk_test_`)
- Для **продакшена** используйте реальные ключи (`pk_live_`, `sk_live_`)
- Webhook в продакшене должен использовать HTTPS
- Не коммитьте секретные ключи в Git!


